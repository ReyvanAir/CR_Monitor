# Mosquitto broker config - classroom monitor project
# LAN / school-project settings. Add authentication before any real deployment.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

# Log to stdout so `docker compose logs mosquitto` shows activity
log_dest stdout
log_type notice
